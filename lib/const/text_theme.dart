
import 'package:flutter/material.dart';

TextStyle diaryTextStyle({
  required double textSize,
  FontWeight? fontWeight,
  Color? color,
}) => TextStyle(fontFamily: "EF_Diary", fontSize: textSize, color: color, fontWeight: fontWeight);


TextStyle gbsTextStyle({
  required double textSize,
  FontWeight? fontWeight,
  Color? color,
}) => TextStyle(fontFamily: "PlaywriteGBS", fontSize: textSize, color: color, fontWeight: fontWeight);